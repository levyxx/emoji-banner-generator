# 🎨 Emoji Banner Generator

テキストを絵文字バナーアートに変換するCLIツールです。

## ✨ 特徴

- 🔥 テキストを絵文字でビットマップ表示
- 📋 ワンクリックでクリップボードにコピー
- 💬 Slack Block Kit JSON形式での出力
- 🎨 GitHubコントリビューショングラフ風テーマ
- 🌈 複数絵文字でのグラデーション表示

## 📦 インストール

### npxで直接実行（推奨）

```bash
npx emoji-banner-generator "Hello" -e 🔥
```

### グローバルインストール

```bash
npm install -g emoji-banner-generator
emoji-banner "Hello" -e 🔥
```

## 🚀 使い方

### 基本的な使い方

```bash
# 直接絵文字を指定
npx emoji-banner-generator "Hello" -e 🔥

# 絵文字エイリアスを使用（コロンあり/なし両対応）
npx emoji-banner-generator "World" -e fire
npx emoji-banner-generator "World" -e :fire:
```

### 複数の絵文字を使用

```bash
# カンマ区切りで複数指定
npx emoji-banner-generator "Rainbow" -e "🔴,🟠,🟡,🟢,🔵,🟣"

# エイリアスでも可能
npx emoji-banner-generator "Colors" -e "fire,star,gem,heart"
```

### 絵文字モード

複数の絵文字を指定した場合の表示モードを選択できます：

```bash
# ランダム（デフォルト）
npx emoji-banner-generator "Random" -e "🔥,⭐,💎" -m random

# 行ごとに絵文字を変更
npx emoji-banner-generator "Rows" -e "🔴,🟠,🟡,🟢" -m row

# 列ごとに絵文字を変更
npx emoji-banner-generator "Cols" -e "🔴,🟠,🟡,🟢" -m column

# 行方向にグラデーション
npx emoji-banner-generator "Gradient" -e "🔴,🟠,🟡,🟢,🔵" -m row-gradient

# 列方向にグラデーション
npx emoji-banner-generator "Gradient" -e "🔴,🟠,🟡,🟢,🔵" -m column-gradient
```

### 背景絵文字を指定

```bash
# 背景を黒い四角で埋める
npx emoji-banner-generator "Hello" -e 🔥 -b ⬛

# 背景も絵文字エイリアスで指定可能
npx emoji-banner-generator "Hello" -e fire -b black_square
```

### GitHubテーマ

```bash
# GitHubのコントリビューショングラフ風に表示
npx emoji-banner-generator "GitHub" --theme github
```

### クリップボードにコピー

```bash
# 生成結果をクリップボードにコピー
npx emoji-banner-generator "Copy Me" -e 🎉 --copy
```

### ファイルからテキストを読み込み

```bash
# ファイルの内容をバナーに変換
npx emoji-banner-generator --file ./message.txt -e 📝
```

### Slack形式で出力

```bash
# Slack Block Kit JSON形式で出力
npx emoji-banner-generator "Slack" -e 🎉 --format slack
```

## ⚙️ オプション一覧

| オプション | 短縮形 | 説明 | デフォルト |
|-----------|--------|------|-----------|
| `--emoji` | `-e` | 使用する絵文字（カンマ区切りで複数可） | 🔥 |
| `--background` | `-b` | 背景の絵文字 | 空白 |
| `--file` | `-f` | テキストを読み込むファイルパス | - |
| `--copy` | `-c` | クリップボードにコピー | false |
| `--format` | - | 出力形式（text, slack） | text |
| `--theme` | - | テーマ（default, github） | default |
| `--mode` | `-m` | 絵文字選択モード | random |
| `--border` | - | 外枠を付ける（絵文字省略時は背景と同じ絵文字を使用） | - |
| `--font` | - | 使用するピクセルフォント名 | block |
| `--help` | `-h` | ヘルプを表示 | - |
| `--version` | `-V` | バージョンを表示 | - |

## 🎨 絵文字選択モード

| モード | 説明 |
|--------|------|
| `random` | 各ドットにランダムな絵文字を配置 |
| `row` | 行ごとに異なる絵文字を使用 |
| `column` | 列ごとに異なる絵文字を使用 |
| `row-gradient` | 行方向にグラデーション |
| `column-gradient` | 列方向にグラデーション |

## 📝 絵文字の指定方法

絵文字は以下の3つの方法で指定できます：

1. **直接絵文字**: `🔥`, `⭐`, `💎`
2. **コロン付きエイリアス**: `:fire:`, `:star:`, `:gem:`
3. **コロンなしエイリアス**: `fire`, `star`, `gem`

## 🛡️ セキュリティ

- パストラバーサル攻撃に対する保護
- 入力サニタイズ
- ファイルサイズ制限（1MB）
- テキスト長制限（100文字）

## 🔧 開発

### 開発環境のセットアップ

```bash
git clone https://github.com/yourusername/emoji-banner-generator.git
cd emoji-banner-generator
npm install
```

### 開発モードで実行

```bash
npm run dev "Hello" -e 🔥
```

### ビルド

```bash
npm run build
```

## 📄 ライセンス

MIT
